# -*- coding: utf-8 -*-
"""Prediction.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1RY6tu-DCXl6j5lh03CRjYYRsMWCEoX7k
"""

import pandas as pd
import numpy as np
# from sklearn.model_selection import train_test_split
# from sklearn.linear_model import LogisticRegression

df = pd.read_csv("employee_data.csv")
df

df.info()

df.isnull().sum()

X = df.drop('Attrition', axis=1)
y = df['Attrition']

X = pd.get_dummies(X, drop_first=True)

# split X and y into training and testing sets

from sklearn.model_selection import train_test_split

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size = 0.3, random_state = 0)

from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()

X_train = scaler.fit_transform(X_train)

X_test = scaler.transform(X_test)

# import KNeighbors ClaSSifier from sklearn
from sklearn.neighbors import KNeighborsClassifier


# instantiate the model
knn = KNeighborsClassifier(n_neighbors=3)


# fit the model to the training set
knn.fit(X_train, y_train)

y_pred = knn.predict(X_test)

y_pred

from sklearn.metrics import classification_report, confusion_matrix, accuracy_score

print("Confusion Matrix:")
print(confusion_matrix(y_test, y_pred))

print("\nClassification Report:")
print(classification_report(y_test, y_pred, zero_division=0))

from sklearn.model_selection import train_test_split
from sklearn.svm import SVC
from sklearn.metrics import classification_report, accuracy_score

# Inisialisasi dan train SVM model
svm_model = SVC(kernel='linear') # bisa menggunakan kernel yang berbeda ('rbf', 'poly', dll)
svm_model.fit(X_train, y_train)

# Membuat prediksi
y_pred = svm_model.predict(X_test)

# Evvaluasi model
print("Accuracy:", accuracy_score(y_test, y_pred))
print(classification_report(y_test, y_pred))

from sklearn.model_selection import train_test_split
from sklearn.svm import SVC
from sklearn.metrics import classification_report, accuracy_score

# Inisialisasi dan train SVM model
svm_model = SVC(kernel='rbf') # bisa menggunakan kernel yang berbeda ('rbf', 'poly', dll)
svm_model.fit(X_train, y_train)

# Membuat prediksi
y_pred = svm_model.predict(X_test)

# Evvaluasi model
print("Accuracy:", accuracy_score(y_test, y_pred))
print(classification_report(y_test, y_pred))

from sklearn.model_selection import train_test_split
from sklearn.svm import SVC
from sklearn.metrics import classification_report, accuracy_score

# Inisialisasi dan train SVM model
svm_model = SVC(kernel='poly') # bisa menggunakan kernel yang berbeda ('rbf', 'poly', dll)
svm_model.fit(X_train, y_train)

# Membuat prediksi
y_pred = svm_model.predict(X_test)

# Evvaluasi model
print("Accuracy:", accuracy_score(y_test, y_pred))
print(classification_report(y_test, y_pred))

import joblib
# Save the trained models
joblib.dump(knn, 'knn_model.pkl')
joblib.dump(svm_model, 'svm_model.pkl')
combined_models = {
    'knn': knn,
    'svm_poly': svm_model,
}
joblib.dump(combined_models, 'model.pkl')

!pip freeze requirements.txt

